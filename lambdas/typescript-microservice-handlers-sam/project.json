{
    "name": "typescript-microservice-handlers-sam",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "lambdas/typescript-microservice-handlers-sam/src",
    "projectType": "application",
    "tags": [],
    "targets": {
        "deploy": {
            "executor": "nx:run-commands",
            "options": {
                "cwd": "{projectRoot}",
                "command": "make deploy"
            }
        },
        "build": {
            "executor": "nx:run-commands",
            "options": {
                "cwd": "{projectRoot}",
                "command": "make build"
            }
        }
    }
}
